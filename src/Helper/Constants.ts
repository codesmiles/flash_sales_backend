export enum CrudOperationsEnum {
    CREATE = "create",
    GETALL = "getAll",
    UPDATE = "update",
    DELETE = "delete",
    FINDSINGLE = "findSingle",
}

export enum UserRoles {
    ADMIN = "admin",
    "USER" = 'user'
}
